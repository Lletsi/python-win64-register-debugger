# python-win64-register-debugger
This project is a Python‑based Windows x64 debugger built with the ctypes library. It interacts directly with the Windows API to attach to processes, enumerate threads, and retrieve CPU register states.
